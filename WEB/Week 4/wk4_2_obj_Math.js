//// Math Methods: 
//// max(x,y), min(x,y), pow(x,y), sqrt(x), ceil(x), floor(x), round(x), 
//// random(x) // return floating point number between 0 (inclusive) and 1 (exclusive)

/* console.log( Math.ceil(0.52) ); // 1
console.log( Math.ceil(0.49) ); // 1
console.log( Math.floor(0.52) ); // 0
console.log( Math.round(0.52) ); // 1
console.log( Math.round(0.49) ); // 0
console.log( Math.round(0.5) );  // 1
console.log( Math.min(0, 5) );  // 0
console.log( Math.max(0, 5) );  */ // 5

///// generate radom numbers
// Returns a random number between 0 (inclusive) and 1 (exclusive), [0..1)
/*
console.log(Math.random());

// (1) Returns a random number between min (inclusive) and max (exclusive)
var max = 6, min =1;
console.log(Math.random() * (max - min) + min);

// (2) Returns a random integer between min (included) and max (excluded)
console.log(Math.floor(Math.random() * (max - min)) + min);

// (3) Returns a random integer between min (included) and max (included) [1..6]
console.log(Math.floor(Math.random() * (max - min + 1)) + min);
 */